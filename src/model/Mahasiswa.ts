export class Mahasiswa {
    public npm:number
    public nama:string
    public ipk:number

    constructor(npm: number, nama: string, ipk: number) {
        this.npm = npm
        this.nama = nama
        this.ipk = ipk
    }
}